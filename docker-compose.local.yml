volumes:
  newschool_local_postgres_data: {}
  newschool_local_postgres_data_backups: {}
  newschool_local_redis_data: {}

services:
  django: &django
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: newschool_local_django
    container_name: newschool_local_django
    depends_on:
      - postgres
      - redis
    volumes:
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - '8000:8000'
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: newschool_production_postgres
    container_name: newschool_local_postgres
    volumes:
      - newschool_local_postgres_data:/var/lib/postgresql/data
      - newschool_local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.postgres

  redis:
    image: docker.io/redis:6
    container_name: newschool_local_redis

    volumes:
      - newschool_local_redis_data:/data


  celeryworker:
    <<: *django
    image: newschool_local_celeryworker
    container_name: newschool_local_celeryworker
    depends_on:
      - redis
      - postgres
    ports: []
    command: /start-celeryworker

  celerybeat:
    <<: *django
    image: newschool_local_celerybeat
    container_name: newschool_local_celerybeat
    depends_on:
      - redis
      - postgres
    ports: []
    command: /start-celerybeat

  flower:
    <<: *django
    image: newschool_local_flower
    container_name: newschool_local_flower
    ports:
      - '5555:5555'
    command: /start-flower

  botservice:
    <<: *django
    image: newschool_local_botservice
    container_name: newschool_local_botservice
    depends_on:
      - redis
      - postgres
    ports: []
    command: python manage.py run_bot
